name: docker-pipeline-boopanapriya
on: push
jobs:
  docker-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print your Register Number
        run: echo "Hello World! - 22IT005"
        
      - name: Checkout 
        uses: actions/checkout@v4

      - name: Docker Build
        run: docker build -t boopanapriya/ml-model-22it005 .

      - name: Docker Images
        run: docker images 
